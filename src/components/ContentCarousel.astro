---
interface Props{
    h: string
    p: string
    href: string
}
const { h, p, href } = Astro.props
---





<h2 class="py-2 font-bold text-xl lg:text-3xl">{h}</h2>
<p class="font-semibold text-xs lg:text-lg">{p}</p>
<a href={href} target="_blank" class="border-2 border-white px-4 py-2 mt-4 hover:bg-white hover:text-black transition font-semibold">Mas aquí</a>
