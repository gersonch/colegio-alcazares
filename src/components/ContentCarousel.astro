---
interface Props{
    h: string
    p: string
}
const { h, p } = Astro.props
---





<h2 class="py-2 font-bold text-2xl lg:text-3xl">{h}</h2>
<p class="font-semibold text-md lg:text-lg">{p}</p>
<a href="#" class="border-2 border-white px-4 py-2 mt-4 hover:bg-white hover:text-black transition font-semibold">Mas aquí</a>
